export const env = {
  adminUsername: process.env.ADMIN_USERNAME ?? 'Admin',
  adminPassword: process.env.ADMIN_PASSWORD ?? 'admin123',
  restrictedUsername: process.env.RESTRICTED_USERNAME,
  restrictedPassword: process.env.RESTRICTED_PASSWORD
};
